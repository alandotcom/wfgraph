{
  "name": "@rova/example-app",
  "version": "0.0.0",
  "private": true,
  "description": "The adopter path written out: a host app that mounts Rova and declares its own Events and actions",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development HOST=127.0.0.1 INNGEST_BASE_URL=http://localhost:8388 tsx watch --env-file-if-exists=../.env.local --env-file-if-exists=../.env app.ts",
    "start": "NODE_ENV=production tsx --env-file-if-exists=../.env.local --env-file-if-exists=../.env app.ts"
  },
  "dependencies": {
    "@rova/client": "workspace:*",
    "@rova/core": "workspace:*",
    "@rova/plugins": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsx": "catalog:"
  }
}
