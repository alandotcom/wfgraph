{
  "name": "@wfgraph/example-app",
  "version": "0.0.0",
  "private": true,
  "description": "The adopter path written out: a host app that mounts Workflow Graph and declares its own Events and actions",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development HOST=127.0.0.1 INNGEST_BASE_URL=http://localhost:8388 INNGEST_CONNECT_GATEWAY_URL=ws://localhost:8390/v0/connect LOG_FORMAT=pretty tsx watch --env-file-if-exists=../.env.local --env-file-if-exists=../.env app.ts",
    "start": "NODE_ENV=production tsx --env-file-if-exists=../.env.local --env-file-if-exists=../.env app.ts"
  },
  "dependencies": {
    "@wfgraph/client": "workspace:*",
    "@wfgraph/core": "workspace:*",
    "@wfgraph/plugins": "workspace:*",
    "hono": "catalog:",
    "inngest": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsx": "catalog:"
  }
}
