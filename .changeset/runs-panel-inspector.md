---
"@wfgraph/client": patch
---

Restyle the editor Runs panel as three surfaces in the Properties language: a wrapping run list, a stacked run overview with an executed-node index, and a node inspector that opens from the canvas without leaving Runs.
