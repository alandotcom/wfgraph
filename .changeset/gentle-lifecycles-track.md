---
"@wfgraph/core": minor
"@wfgraph/client": minor
---

Improve Lifecycle configuration with clearer run behavior, optional Entity eligibility, binding diagnostics, and compact outcome summaries.

Add enum set conditions with multi-select values and add the `entityResolverTimeoutMs` host option, which defaults to 10 seconds.
